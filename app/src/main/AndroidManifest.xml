<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.READ_PHONE_STATE"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        tools:node="remove" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />


    <application
        android:name=".EffectInstance"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.Dark"
        android:usesCleartextTraffic="false"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:ignore="GoogleAppIndexingWarning"
        tools:replace="android:icon">


        <activity
            android:name=".MainActivity"
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.media.action.DISPLAY_AUDIO_EFFECT_CONTROL_PANEL" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.CATEGORY_CONTENT_MUSIC" />
            </intent-filter>
        </activity>

        <service
            android:name=".ForegroundService"
            android:exported="false"
            android:permission="android.permission.FOREGROUND_SERVICE" />

        <activity
            android:name=".AboutActivity"
            android:label="About"
            android:exported="false">
            android:parentActivityName=".MainActivity">
        </activity>
        <activity
            android:name=".SettingsActivity"
            android:label="Settings"
            android:exported="false"
            android:parentActivityName=".MainActivity">

        </activity>
        <activity android:name=".SupportActivity"
            android:label="Support"
            >
        </activity>
        <!-- full‑screen diagnostics activity copied from earlier release; provides
             step‑by‑step log collection, sanitization, and optional email sending. -->
        <activity android:name=".DebugActivity"
                  android:exported="false" />
        <!-- provider needed for attaching debug log files to email -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        <receiver android:name="com.onesignal.UpgradeReceiver" tools:node="remove" />
        <!-- Onesignal UpgradeReceiver is removed to prevent proprietary push/analytics code.
             It normally handles upgrading the OneSignal SDK; removing it ensures no
             closed‑source library is included. -->
    </application>

</manifest>
