<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- deny all cleartext traffic by default -->
    <base-config cleartextTrafficPermitted="false" />
    <!-- block all external network requests (no INTERNET permission granted) -->
</network-security-config>